FRONTEND: (REACT JS)

    TO RUN:
        -navigate to frontend folder
        npm start

    TO CREATE:
        npx create-react-app .


    DOWNLOAD PACKAGES:
        npm i @fortawesome/fontawesome-svg-core": "^6.1.1
        npm i @fortawesome/free-solid-svg-icons": "^6.1.1
        npm i @fortawesome/react-fontawesome": "^0.1.18

        npm i axios

        npm i antd


        VIDEO 16 CREATE CONTEXT
        AND
        VIDEO 25 CREATE SIGNIN



BACKEND: (NODE JS/EXPRESS)

    TO RUN:
        -navigate to backend folder
        node server

    TO CREATE:
        npm init

    DOWNLOAD PACKAGES:
        npm i nodemon
        npm i mongoose
        npm i express

        npm i dotenv

        npm i cors

        npm i bcryptjs

        npm i jsonwebtoken

        npm i express-async-handler